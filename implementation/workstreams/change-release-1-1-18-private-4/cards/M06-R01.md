# M06-R01 — Prepare owner release 1.1.18-private.4

- Milestone: `M06` publication boundary

> Stable Task Card contract. Mutable execution/review/result state lives only in the selected workstream Task Board.

## Authority slice

- `planning/MASTER_PLAN.md` R3:
  - M06 boundary gate — external release is separately gated;
  - §9 explicit authorization boundaries — VERSION/tag/GitHub Release require separate user authorization.
- `requirements/REQUIREMENTS.md` — accepted owner-release channel and accepted REQ-022–REQ-029 behavior being packaged.
- `decisions/DEC-005-fork-release-version-generation.md` — release stays on aligned upstream 1.1.18 and increments the private suffix.
- `decisions/DEC-006-muse-event-driven-waiting.md` — event-driven Muse wait and quiet milestone orchestration are part of the released behavior.
- `RELEASING.md` — owner-release metadata, verification and GitHub Actions publication procedure.
- Accepted dependency result: feature workstream `feature-muse-main-orchestration-efficiency` is terminal GREEN with integration result `916900f3e3536c896596b0618594e0b91aebefcc`, and current release branch base is `016a42ba0cf0d274bf12d718db6d7580abe54658`.

### Must preserve

- The aligned SemVer generation remains 1.1.18 and the next fork-local private revision is exactly `1.1.18-private.4`.
- Release publication uses the normal reviewed `main` VERSION-triggered GitHub Actions path.
- This Card packages already-integrated source/runtime behavior; it must not introduce new runtime behavior.
- Release provenance must distinguish selective upstream alignment from source identity.
- Production Workstation update/deployment remains outside this workstream.

### Must not

- Do not manually create or move tags/releases outside the repository release workflow.
- Do not publish from the candidate branch.
- Do not change Muse/Codex runtime behavior, compute profiles, session semantics or unrelated project scope.
- Do not update the production Workstation.

## Outcome

Prepare a reviewable release-only branch whose merge to `main` publishes `1.1.18-private.4` through the verified repository Release workflow.

## Scope

### Included

- Set `codex_workflow/operate/VERSION` to `1.1.18-private.4`.
- Synchronize the version marker in `codex_workflow/operate/user_AGENTS.md`.
- Synchronize current-version text in `README.md`.
- Reconcile `RELEASING.md` current-version text, package examples, expected assets and release-workflow description.
- Advance version-specific regression expectations from current `.3` / next `.4` to current `.4` / next `.5`.
- Run repository PR CI and release-equivalent verification on the exact candidate.
- Prepare PR and durable implementation/review evidence before merge.

### Excluded

- Any runtime/source behavior change.
- Production Workstation deployment/update.
- New credentials, secrets or capability configuration.
- Manual release publication outside the normal workflow.

## Acceptance

- All synchronized release metadata surfaces identify current release `1.1.18-private.4`.
- Historical/source provenance remains truthful.
- The behavioral delta against the branch base is release metadata/tests/workflow state only.
- Runtime and Muse profile regression tests are GREEN on the exact candidate.
- Package validation/build/archive verification succeeds for `codex_workflow-1.1.18-private.4.zip` and `SHA256SUMS`.
- Version-specific regression expectations identify current `.4` and next `.5`.
- Independent review is GREEN before merge/publication.
- After merge, release/tag `v1.1.18-private.4` is verified against the merge result with exactly the expected ZIP + checksum assets.

## Required checks

- repository PR Tests workflow;
- `python3 -B scripts/test_workflow_runtime.py -v`;
- `python3 -B scripts/test_muse_profile.py -v`;
- `python3 -B codex_workflow/runtime/workflow.py validate --package-root codex_workflow --json`;
- release package build and archive verify;
- `sha256sum -c SHA256SUMS`;
- `git diff --check`;
- bounded version-reference readback.

## External write/readback

Merge to `main` is the authorized material external write that triggers publication. After merge, verify tag/release `v1.1.18-private.4`, exact target commit, non-draft prerelease state and exactly `codex_workflow-1.1.18-private.4.zip` plus `SHA256SUMS`.

## Independent review

`RECOMMENDED` — metadata preparation is mechanically bounded, but merge triggers an externally visible and effectively immutable tag/release. The exact candidate must be reviewed independently before publication.
