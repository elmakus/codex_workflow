# M04-T02 implementation evidence

Status: implementation and integrated verification complete; independent review pending
Review subject: `9a0761f8b0159e63d8c12a95660d5874007b790a`
Execution-start state: `42fce30b8118a20f628eb2d0afb3b7252958fcb1`
Predecessor: terminal GREEN `M04-T01` at `53ca7664b625b6467ffdc13d5d5f9a99675a5269`
Pull request: #7

## Result

The integrated M04 residual scan found one bounded active-documentation defect after M04-T01: `RELEASING.md` still described Companion plus the retired `luna-xhigh` / `pro-x5` profiles and commands.

The exact review subject corrects only that residual documentation surface and adds a focused regression guard:

- `RELEASING.md` now states exactly two supported profiles, `plus` and `muse-max`;
- it names only the accepted six worker roles and aligns their plus/Muse runtime semantics with the accepted architecture;
- retired profile commands and Companion language are removed;
- `scripts/test_workflow_runtime.py` now guards the release guide against reintroducing removed profiles/roles, the legacy workflow-breakdown filename, or `multi_agent_v2` documentation ownership.

No runtime/profile implementation, update semantics, VERSION, tag, release, or publication behavior changed.

## Residual exact-source verification

At exact subject `9a0761f8b0159e63d8c12a95660d5874007b790a`, the active operator/public/orchestration set inspected was:

- `README.md`
- `workflow_breakdown.md`
- `benchmarks/README.md`
- `RELEASING.md`
- `codex_workflow/AGENTS.md`
- `codex_workflow/heavy_route.md`
- `codex_workflow/delegation.md`
- `codex_workflow/operate/user_AGENTS.md`

That surface is clean of active references to the legacy `workflow_break_down.md` filename, `luna-xhigh`, `pro-x5`, Companion, Micro Executor/Micro Execution, Deployment Token Report, and `multi_agent_v2`.

Additional exact-source readback confirms:

- `codex_workflow/runtime/compute_profiles.py` exposes only `plus` and `muse-max`;
- `codex_workflow/runtime/platform_settings.py` references `features.multi_agent_v2` only as legacy owned keys to remove, while current ownership keeps `features.multi_agent`;
- `codex_workflow/operate/VERSION` remains `1.1.17-private.12`.

## Integrated verification

GitHub Actions Tests run #187 (run id `35500001757`) completed GREEN on the exact review subject.

The exact-subject job reports:

- runtime regression suite: GREEN, 97/97;
- Muse adapter regression suite: GREEN, 33/33;
- Python compile check: GREEN;
- Muse Max profile regression suite: GREEN, 7/7;
- package validation: GREEN;
- package build: GREEN;
- archive verification: GREEN.

The runtime suite includes GREEN coverage for project-only equal-version update behavior, historical-source project catch-up, explicit downgrade protection, fail-closed legacy local-instruction migration, removal of retired architecture assets/routes, current two-profile/operator documentation, and removal of workflow-owned V2 gate settings.

Package build remained non-publishing and produced the existing version `1.1.17-private.12`.

## Review boundary

M04-T02 is contracted as RECOMMENDED independent review. This chat performed the bounded residual correction that forms the exact review subject, so the Card remains non-terminal with `review_state: pending` until a fresh independent reviewer records GREEN/RED evidence.
