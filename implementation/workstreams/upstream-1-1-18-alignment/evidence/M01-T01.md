# M01-T01 implementation evidence

Status: ready for independent review
Review subject: commit `a3f848ede935a8dc39e22eecbd8255dfee262c05`
Reviewed tree: `f06e75e4a6caedd536bdd61b415010dcd6efcea3`

## Result

Implemented the R2/M01 same-version project-only update path:
- release-selected equal-version updates reuse installed verified source and do not call release acquisition;
- equal-version updates use a dedicated project-only lifecycle plan rather than full shared-runtime update planning;
- unchanged project mutations are filtered before apply;
- backups contain only existing project files that will actually change;
- current projects become a true no-op with no backup;
- recorded historical source remains authoritative for older projects;
- project-newer-than-target remains fail-closed without explicit `--allow-downgrade`;
- shared user-level runtime/worker/config/install-state content is preserved on the project-only path.

OpenSpec: `openspec/changes/m01-project-only-update/`.

## Verification

GREEN:
- focused project-only/historical-source/downgrade regression set: 6/6;
- fork runtime suite: `python3 -B scripts/test_workflow_runtime.py` → 94/94;
- `git show --check` on the tested implementation commit: clean;
- `codex_workflow/operate/VERSION`: unchanged;
- local tested implementation tree `f06e75e4a6caedd536bdd61b415010dcd6efcea3` exactly equals the GitHub review-subject tree.

The final runtime suite also verifies the updated owner-release fixtures: newer releases still acquire/delegate, explicit newer `--source` still skips release discovery, while equal-version behavior is covered by the new project-only regressions.
