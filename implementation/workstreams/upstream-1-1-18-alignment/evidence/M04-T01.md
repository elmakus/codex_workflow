# M04-T01 implementation evidence

Status: implementation complete; independent review pending
Review subject: `53ca7664b625b6467ffdc13d5d5f9a99675a5269`
Execution-start state: `83b31b442d203f837e0b42923590527d43ede792`
Predecessor M03 checkpoint: `3e6f86cb78b54c8690b386d8575fc2647465eed3`
Pull request: #7

## Result

The exact subject completes the deterministic documentation/rename portion of M04:

- renamed the canonical root architecture guide from `workflow_break_down.md` to `workflow_breakdown.md`;
- refreshed the guide header to the current packaged `1.1.17-private.12` fork and added an adapted orchestration deep dive;
- the deep dive describes the accepted six-role topology, proportionate context intake, exact-three Investigator lanes, direct worker→Main results, two-profile runtime split, plus-only bounded material events, Tester independence/repair ordering, and non-polling coordination;
- added `benchmarks/README.md` as broader benchmark-coverage guidance built on the existing light benchmark, without presenting a single case as a general ranking;
- updated the root README to the canonical architecture filename and benchmark guide;
- updated focused runtime/document-contract regression coverage to require the canonical filename, absence of the legacy filename, the adapted deep-dive surface, and supported benchmark profile names.

No runtime/profile mapping, worker lifecycle, update semantics, VERSION, release, tag, or publication behavior changed.

## Verification

Exact review subject `53ca7664b625b6467ffdc13d5d5f9a99675a5269`:

- GitHub Actions Tests run #174 (run id `35499157865`): GREEN.
- Runtime regression suite: GREEN (97 tests; the job step completed successfully).
- Muse adapter regression suite: GREEN.
- Python compile check: GREEN.
- Muse Max profile regression suite: GREEN.
- Package validation: GREEN.
- Package build: GREEN.
- Archive verification: GREEN.
- Exact-source readback: `workflow_breakdown.md` exists and `workflow_break_down.md` is absent.
- Exact-source scan across `README.md`, `workflow_breakdown.md`, and `benchmarks/README.md`: no active `luna-xhigh`, `pro-x5`, Companion, Micro Executor/Micro Execution, `multi_agent_v2`, or legacy filename references.
- Changed-surface whitespace/conflict-marker scan: no trailing-whitespace lines and no merge-conflict markers in `README.md`, `workflow_breakdown.md`, `benchmarks/README.md`, or `scripts/test_workflow_runtime.py`.
- `codex_workflow/operate/VERSION`: unchanged at `1.1.17-private.12`.

Two earlier CI attempts (#172 and #173) failed only because newly added prose assertions expected phrases to be contiguous across Markdown line wrapping. The assertions were normalized without changing the documented contract; run #174 is GREEN on the corrected exact subject.

## Review boundary

M04-T01 is contracted as RECOMMENDED independent review. This chat implemented the exact subject, so the Card remains non-terminal with `review_state: pending` until a fresh independent reviewer records GREEN/RED evidence.
