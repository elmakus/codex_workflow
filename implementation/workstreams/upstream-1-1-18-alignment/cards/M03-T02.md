# M03-T02 — Plus-only Material Event Push and two-profile communication contract

- Milestone: `M03`

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#M03--two-profile-execution-and-communication-semantics`
- Requirements: `REQ-008`, `REQ-011`, `REQ-012`, `REQ-013`, `REQ-014`, `REQ-015`, `REQ-019`, `REQ-021`
- Accepted decisions: `decisions/DEC-002-compute-profiles.md`, `decisions/DEC-003-communication-context.md`, `decisions/DEC-004-selective-upstream-adoption.md`
- Dependency result: terminal GREEN `M03-T01` exact result/review subject when available.
- Predecessor M02 handoff: `implementation/workstreams/upstream-1-1-18-alignment/handoffs/M02_HANDOFF.md`
- Research: `research/upstream-1.1.18-audit.md`
- OpenSpec: none; this Card reconciles orchestration text to already-accepted communication/profile authority.

### Must preserve

The exact six-role M02 topology, direct worker→Main final reports, exact-three Investigator lanes, Tester independence, proportionate documentation intake, Explorer bounded broader-context role, Muse session safety and Executor→Tester→repair→recheck ordering remain intact. `plus` remains internal Codex; `muse-max` remains fully Muse-backed. `multi_agent_v2` remains non-owned/removed. VERSION remains unchanged.

### Must not / rationale that must travel

Do not specify, emulate or retain a negative pseudo-contract for Material Event Push under `muse-max`; DEC-003 requires Muse-specific unavailable-push text to be removed rather than preserved as active policy. Do not permit sibling messaging. Do not reintroduce removed profiles/roles, mandatory full-`agent_docs/` intake, V2 timeout ownership, VERSION/release changes, or wholesale upstream adoption.

## Dependencies

- `M03-T01` terminal GREEN.

## Outcome

Active orchestration and operator-facing profile communication surfaces describe one coherent two-profile contract: plus-only bounded Material Event Push for internal Codex workers, and Muse-backed `muse-max` without a Material Event Push contract/emulation.

## Scope

### Included
- Reconcile `codex_workflow/AGENTS.md`, `heavy_route.md` and `delegation.md` to only plus/muse-max harness semantics.
- Scope Material Event Push positively to `plus` internal Codex workers and keep BLOCKER / COURSE_CHANGE / CRITICAL_PARTIAL worker→Main semantics.
- Remove Muse-specific unavailable-push/emulation policy text rather than retaining it as a negative contract.
- Remove stale `luna-xhigh` / `pro-x5` supported-path references from active orchestration and operator/public surfaces that would otherwise advertise those profiles.
- Reconcile directly affected runtime/profile/document-contract tests while preserving proportionate context intake and Explorer usage.
- Clean the existing duplicate consecutive `Worker Follow-up and Repair` heading when touching `delegation.md`.

### Excluded
- M04 filename rename, benchmark/deep-dive adaptation and residual integrated documentation cleanup.
- VERSION/release-triggering changes or publication.

## Acceptance

- Active orchestration/profile surfaces name only `plus` and `muse-max` as supported profiles.
- All six roles are internal Codex workers under plus and Muse logical workers under muse-max, with no internal Companion/profile exception.
- Material Event Push is specified only for plus internal workers, only for BLOCKER / COURSE_CHANGE / CRITICAL_PARTIAL, and only worker→Main.
- No active muse-max contract specifies unavailable Material Event Push, `send_message` emulation, polling shim or equivalent negative policy.
- Direct sibling messaging remains absent; normal/final reports return to Main.
- Proportionate documentation intake and Explorer bounded discovery remain authoritative; mandatory complete session-level `agent_docs/` intake remains absent.
- Platform setting non-ownership of `multi_agent_v2` and VERSION remain unchanged.
- No duplicate `Worker Follow-up and Repair` heading remains.

## Required tests / checks

Update focused contract/profile regressions for two-profile naming, plus-only Material Event Push, absence of Muse push/emulation policy, direct Main routing, proportionate docs intake and removed-profile references. Run affected runtime/Muse/profile suites, package validation/build verification, static active-surface scans and `git diff --check`.

## External write/readback needs

Repository branch only. No live deployment, VERSION bump, tag or release publication.

## Independent review

`REQUIRED` — this changes the active worker communication/runtime-selection contract used during substantive execution.
