# M03-T01 — Two-profile runtime and profile command surface

- Milestone: `M03`

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#M03--two-profile-execution-and-communication-semantics`
- Requirements: `REQ-011`, `REQ-012`, `REQ-013`, `REQ-019`, `REQ-021`
- Accepted decisions: `decisions/DEC-002-compute-profiles.md`, `decisions/DEC-004-selective-upstream-adoption.md`
- Predecessor checkpoint: M02 GREEN at `19d27ec9dce2f6e6a452d09799ec0871c6efff01`
- Predecessor handoff: `implementation/workstreams/upstream-1-1-18-alignment/handoffs/M02_HANDOFF.md`
- Research: `research/upstream-1.1.18-audit.md`
- OpenSpec: none; accepted requirements/decisions already define the supported profile/runtime contract.

### Must preserve

The six-role M02 worker set remains exact. Under `plus`, all six roles use the internal Codex lifecycle. Under `muse-max`, all six roles use the retained Muse logical-session/process lifecycle with existing binding, lease, resume, timeout/cancellation and Tester-independence safety. Main remains user-selected. Existing `multi_agent = true` ownership and removal/non-ownership of `multi_agent_v2` settings remain unchanged. VERSION and release markers remain unchanged.

### Must not / rationale that must travel

Do not retain `luna-xhigh` or `pro-x5` as aliases, hidden compatibility profiles, CLI choices or test-only supported paths. Do not reintroduce Companion/Micro roles. Do not change Muse adapter lifecycle semantics merely to remove profiles. Do not perform T02 communication-policy cleanup beyond what the profile renderer/command requires. Do not adopt upstream wholesale or change VERSION/publish/release.

## Dependencies

- M02 GREEN checkpoint `19d27ec9dce2f6e6a452d09799ec0871c6efff01`.

## Outcome

Runtime/profile authority and the profile operator command expose exactly two supported profiles: `plus` and `muse-max`, each mapped to the exact six-role M02 topology.

## Scope

### Included
- Reduce `runtime/compute_profiles.py` supported profile and renderer authority to `plus` and `muse-max`.
- Remove `luna-xhigh` / `pro-x5` profile-specific allocations, communication-renderer branches and compatibility behavior.
- Reconcile `operate/profile.md` to the two-profile command surface and the actual six-role plus/muse-max mappings.
- Reconcile focused runtime/Muse profile tests to prove exactly two supported profiles, rejection of removed profile names, six Codex-backed roles under plus and six Muse-backed roles under muse-max.
- Preserve Muse role enumeration and platform-setting non-ownership behavior.

### Excluded
- Final AGENTS/Heavy/delegation Material Event Push and context-policy cleanup owned by `M03-T02`.
- Broader documentation rename/deep-dive adaptation owned by M04, except the authoritative profile operator procedure changed here.
- VERSION/release-triggering changes or publication.

## Acceptance

- `COMPUTE_PROFILES` and profile communication-renderer authority contain only `plus` and `muse-max`.
- Removed profile names fail validation and are absent from profile switch tests/accepted CLI procedure.
- `plus` maps exactly all six M02 roles to internal Codex allocations; `muse-max` maps exactly all six roles to `muse-code` with Muse Spark 1.3 Contributor / max.
- Profile switching/bootstrap/update preservation tests cover only supported profiles and continue to fail closed on invalid values.
- Muse adapter/session safety behavior is unchanged and its profile authorization still rejects non-Muse allocations.
- Generated platform configuration still retains `multi_agent = true` without workflow-owned `multi_agent_v2` timeout settings.
- VERSION/release markers remain unchanged.

## Required tests / checks

Run focused compute-profile tests in `scripts/test_workflow_runtime.py` and `scripts/test_muse_profile.py`, Muse adapter regressions when affected, platform-settings regressions, full configured CI/package validation, static removed-profile scans over runtime/profile command/test surfaces, and `git diff --check` equivalent.

## External write/readback needs

Repository branch only. No live deployment, VERSION bump, tag or release publication.

## Independent review

`REQUIRED` — this changes the supported runtime profile set and worker-harness routing authority.
