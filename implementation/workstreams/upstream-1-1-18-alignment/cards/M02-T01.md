# M02-T01 — Six-role worker/package baseline

- Milestone: `M02`

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#M02--six-role-worker-topology-and-investigation-contract`
- Requirements: `REQ-005`, `REQ-006`, `REQ-009`, `REQ-010`, `REQ-020`, `REQ-021`
- Accepted decisions: `decisions/DEC-001-worker-topology.md`, `decisions/DEC-004-selective-upstream-adoption.md`
- Predecessor checkpoint: M01 GREEN at `3f7aa3c67a564216a6095b5ad92a3db2589ef8a1`
- Predecessor handoff: `implementation/workstreams/upstream-1-1-18-alignment/handoffs/M01_HANDOFF.md`
- Research: `research/upstream-1.1.18-audit.md`
- OpenSpec: none; this Card reconciles accepted worker/package topology without introducing a new persisted-state or CLI contract.

### Must preserve

Senior Executor, Tester and Archivist remain supported. Current profile names remain intact during M02; profile-count simplification belongs to M03. Existing Muse process/session safety, owner-release/update behavior, package validation safety, unrelated user-owned workers/settings/files, proportionate documentation intake, and the absence of Medium route / Deployment Token Report remain intact.

### Must not / rationale that must travel

Do not retain Companion or Micro Executor as supported aliases/compatibility roles. Do not start M03 profile-removal or plus-only Material Event Push cleanup. Do not change `codex_workflow/operate/VERSION` or synchronized release markers, publish/tag/release, import upstream wholesale, or weaken package/source validation.

## Dependencies

- M01 GREEN checkpoint `3f7aa3c67a564216a6095b5ad92a3db2589ef8a1`

## Outcome

The package/runtime worker definition surface has exactly six supported roles: Explorer, Investigator, Default Executor, Senior Executor, Tester and Archivist.

## Scope

### Included
- Add the Explorer worker template with bounded read-only project-context discovery/mapping/evidence-retrieval responsibility.
- Remove Companion and Micro Executor templates and supported-role registrations.
- Reconcile runtime/package worker-set authority, current compute-profile worker maps, release-package validation and directly affected profile/package tests to the six-role set.
- Remove hard fixed report word ceilings from active worker templates; use smallest-complete evidence-linked decision-ready reporting language.
- Preserve current M02 transitional profile names while making their worker sets coherent with the six-role topology.

### Excluded
- AGENTS/Heavy/delegation orchestration rewrite, exact three-lane Investigator dispatch and sibling-message contract cleanup owned by `M02-T02`.
- Removal of `luna-xhigh` / `pro-x5`, final plus/muse-max communication semantics, or broader Muse profile cleanup owned by M03.
- M04 documentation rename/deep-dive work.
- VERSION/release-triggering changes or publication.

## Acceptance

- Package agent templates and runtime `BUILTIN_WORKERS` contain exactly `explorer`, `investigator`, `default_executor`, `senior_executor`, `tester`, `archivist`.
- `companion` and `micro_executor` are absent from supported worker definitions and release-package required worker set; stale packaged copies are rejected/retired rather than accepted.
- Explorer is read-only and its worker contract matches REQ-006.
- Investigator remains read-only; active worker templates impose no fixed report word-count ceilings and require smallest-complete evidence-linked decision-ready results.
- Existing profile names still validate in M02, but every current profile maps exactly the six supported roles; no Companion/Micro exception survives in profile worker-set authority.
- Senior Executor, Tester and Archivist remain present with their existing responsibility boundaries.
- Medium route and Deployment Token Report remain absent; VERSION/release markers remain unchanged.

## Required tests / checks

Update and run focused worker-set/profile/package validation tests, including `scripts/test_workflow_runtime.py`, `scripts/test_muse_profile.py` and non-publishing package validation/build verification as materially affected. Include a static package/agent-set check proving Companion/Micro absence and Explorer presence. Run `git diff --check` or equivalent whitespace validation on the implementation subject.

## External write/readback needs

Repository branch only. No live deployment, VERSION bump, tag or release publication.

## Independent review

`REQUIRED` — this changes the supported worker/package topology and profile worker-set authority that downstream M03 depends on.
