# M01-T02 — Fail-closed legacy local-instruction migration

- Milestone: `M01`

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#M01--project-only-update-and-legacy-migration-hardening`
- Requirements: `REQ-016`, `REQ-021`
- Accepted decision: `decisions/DEC-004-selective-upstream-adoption.md`
- Research: `research/upstream-1.1.18-audit.md`
- OpenSpec: JIT before implementation if the CLI/migration contract needs a dedicated behavior spec.

### Must preserve

Accepted project-local instructions remain user-owned; migration must fail closed on ambiguous/stale workflow-owned route references. Reviewed local instructions are explicit input, never inferred from stale workflow text. Existing bootstrap/install/update safety remains intact.

### Must not / rationale that must travel

Do not silently rewrite stale route references, infer user intent, change update source/downgrade semantics from T01, or touch release-triggering VERSION metadata.

## Dependencies

- none

## Outcome

Bootstrap, install and update consistently accept explicitly reviewed legacy local instructions while stale removed-route references are rejected before migration.

## Scope

### Included
- CLI plumbing for reviewed legacy local instructions across bootstrap/install/update.
- Central stale legacy workflow-route validation.
- Focused migration/regression coverage.

### Excluded
- Same-version project-only update mechanics owned by M01-T01.
- M02+ topology/profile changes and release publication.

## Acceptance

- Stale protected/project-local instructions referencing removed legacy route files fail closed.
- Reviewed local-instruction input is supported consistently by bootstrap, install and update.
- Accepted local content is preserved exactly apart from workflow-owned rendering boundaries.
- Existing managed-region drift/reserved-marker protections remain GREEN.

## Required tests / checks

Add focused bootstrap/install/update stale-route and reviewed-input regressions; rerun affected runtime suite.

## External write/readback needs

Repository branch only; no live deployment or release publication.

## Independent review

`REQUIRED` — this changes fail-closed migration and user-owned instruction preservation behavior.
