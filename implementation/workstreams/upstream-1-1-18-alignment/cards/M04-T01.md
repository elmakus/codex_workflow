# M04-T01 — Canonical workflow documentation and adapted upstream docs

- Milestone: `M04`

## Authority slice

- Master Plan: `planning/MASTER_PLAN.md#M04--documentation-packaging-and-integrated-regression-closure`
- Requirements: `REQ-017`, `REQ-018`, `REQ-020`, `REQ-021`, plus documentation consistency for the already-accepted M02/M03 architecture.
- Accepted decisions: `decisions/DEC-001-worker-topology.md`, `decisions/DEC-002-compute-profiles.md`, `decisions/DEC-003-communication-context.md`, `decisions/DEC-004-selective-upstream-adoption.md`.
- Predecessor checkpoint: terminal GREEN M03 checkpoint `3e6f86cb78b54c8690b386d8575fc2647465eed3`.
- Predecessor handoff: `implementation/workstreams/upstream-1-1-18-alignment/handoffs/M03_HANDOFF.md`.
- Research: `research/upstream-1.1.18-audit.md`.
- Upstream reference evidence: upstream 1.1.18-era light-benchmark/benchmark proposal and orchestration deep-dive commits identified by the accepted research; upstream text is reference evidence, not direct authority.
- OpenSpec: none; this Card changes documentation/file naming and regression assertions, not a new runtime behavior contract.

### Must preserve

The six-role topology, exact-three Investigator lanes, direct worker→Main reporting, Tester independence, two supported profiles (`plus`, `muse-max`), plus-only bounded Material Event Push, Muse logical-session/process lifecycle, proportionate documentation intake, Explorer bounded discovery, project/update guarantees from M01, absence of Medium route/Deployment Token Report, and non-ownership of `multi_agent_v2` remain intact. VERSION remains unchanged.

### Must not / rationale that must travel

Do not copy upstream documentation verbatim when it describes removed Medium routing, removed roles/profiles, different model allocations, mandatory full-`agent_docs/` intake, sibling messaging, or upstream `multi_agent_v2` timeout ownership. Do not perform release-triggering VERSION changes, tag/release publication, or unrelated runtime refactors.

## Dependencies

- M03 terminal GREEN checkpoint.

## Outcome

Repository/operator documentation uses canonical `workflow_breakdown.md`, contains an architecture-correct adapted orchestration deep dive and broader benchmark-coverage guidance, and has no stale active reference to the old filename or removed architecture.

## Scope

### Included

- Rename root `workflow_break_down.md` to `workflow_breakdown.md`.
- Reconcile active references/tests that still point at the old filename.
- Add an adapted deep-dive section to the canonical workflow breakdown that explains current Main/worker ownership, context control, six-role topology, two-profile runtime split, plus-only material events, independent Tester lifecycle, and recovery/closure without importing rejected upstream semantics.
- Add an architecture-correct `benchmarks/README.md` proposal building on the existing `light_benchmark/` case study.
- Link the canonical workflow breakdown and benchmark-coverage guidance from the root README where appropriate.
- Update focused documentation/contract regressions needed to guard the canonical filename and final architecture.

### Excluded

- New runtime/profile behavior.
- M04 integrated full-suite closure/residual cleanup that depends on this Card's resulting reference scan; materialize that work JIT after this Card is terminal.
- VERSION/release-triggering changes or publication.

## Acceptance

- `workflow_breakdown.md` exists as the canonical root architecture guide and `workflow_break_down.md` is absent.
- Active README/tests/documentation references use the canonical filename.
- The deep-dive material describes only the accepted six-role/two-profile fork architecture and does not reintroduce Medium route, Deployment Token Report, Companion, Micro Executor, retired profiles, sibling messaging, mandatory full-doc intake, or workflow-owned `multi_agent_v2` timeouts.
- `benchmarks/README.md` exists, treats the existing light benchmark as an initial case study, and proposes broader evaluation without claiming unsupported benchmark conclusions.
- Existing active communication/profile contracts remain semantically unchanged.
- VERSION remains `1.1.17-private.12`.

## Required tests / checks

- Update/run focused runtime/document-contract regression coverage for canonical filename and final docs architecture.
- Run affected runtime/Muse/profile regression suites plus package validation/build/archive verification.
- Static scan of active operator/public/orchestration docs for old filename and removed architecture terms.
- `git diff --check` equivalent whitespace/conflict-marker validation through available exact-source/CI evidence.

## External write/readback needs

Repository branch only. No live deployment, VERSION bump, tag or release publication.

## Independent review

`RECOMMENDED` — this is a substantive operator/documentation architecture change and canonical-file rename, but not a high-risk runtime or destructive change.
