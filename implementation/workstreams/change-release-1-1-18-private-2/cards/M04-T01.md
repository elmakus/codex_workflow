# M04-T01 — Prepare owner release 1.1.18-private.2

- Milestone: `M04`

> Stable Task Card contract. Mutable execution/review/result state lives only in the selected workstream Task Board.

## Authority slice

- Master Plan / milestone contract:
  - `planning/MASTER_PLAN.md#4-milestones` — M04 release-readiness outcome;
  - `planning/MASTER_PLAN.md#7-deployment--migration--rollback-strategy`;
  - `planning/MASTER_PLAN.md#10-explicit-authorization-boundaries`.
- Requirements: `requirements/REQUIREMENTS.md` — owner-release channel constraint and REQ-021 selective-adoption boundary.
- Accepted decisions: `decisions/DEC-005-fork-release-version-generation.md`.
- Release procedure: `RELEASING.md`.
- Relevant OpenSpec: none.
- Accepted dependency results:
  - structured Muse capability-hint workstream terminal on `main`;
  - merged integration result `1b297620f19ff495034ad160465ac90fa29b8ed8`;
  - release branch base `760a595125335411288cf3a673356ccdd8ea47af`.

### Must preserve

- The SemVer generation remains 1.1.18 and the next private release is exactly `1.1.18-private.2`.
- Normal owner releases come from reviewed `main` through the repository Release workflow and verified release assets.
- The release packages the already-reviewed current runtime/source; this Card must not introduce new runtime behavior.
- Historical provenance references, including prior `1.1.17-private.13` and `1.1.18-private.1` history, remain truthful and are not blindly rewritten.
- Release metadata checked by `.github/workflows/release.yml` remains synchronized.
- User authorization covers release publication only; production Workstation update remains a separate live-write authorization gate.

### Must not / rationale that must travel

- Do not install or update the production Workstation in this Card/workstream.
- Do not manually create a tag/release to bypass the normal VERSION-triggered Release workflow.
- Do not publish from an unreviewed candidate branch.
- Do not change runtime code, capability semantics, compute profiles, update-channel behavior, or unrelated documentation.

## Dependencies

- Current reviewed/merged `codex_workflow/main` source at branch base.
- Explicit user authorization for release publication: satisfied in the current user decision.

## Outcome

Prepare a reviewable release-only branch whose merge to `main` will publish `1.1.18-private.2` through the normal verified GitHub Actions release path.

## Scope

### Included

- Set `codex_workflow/operate/VERSION` to `1.1.18-private.2`.
- Synchronize the version marker in `codex_workflow/operate/user_AGENTS.md`.
- Synchronize current-version text in `README.md`.
- Reconcile `RELEASING.md` current-version text, package examples, expected assets, and release-workflow description to `1.1.18-private.2` while preserving historical provenance.
- Run release-equivalent non-publishing verification.
- Prepare the branch/PR and durable review evidence needed before merge.

### Excluded

- Runtime/source behavior changes.
- Production Workstation deployment/update.
- New credentials, secrets, capability configuration, or Muse state mutation.
- Manual GitHub tag/release publication outside the repository Release workflow.

## Acceptance

- All four synchronized release metadata surfaces identify current release `1.1.18-private.2`.
- Historical references remain semantically correct.
- No runtime/code behavior changes are present.
- Runtime regression tests are GREEN.
- Muse profile regression tests are GREEN.
- Package validation succeeds.
- Release archive builds and verifies as `codex_workflow-1.1.18-private.2.zip` with `SHA256SUMS`.
- Diff/reference scan shows no stale current-release `1.1.18-private.1` marker in active release metadata.
- Independent review is GREEN before merge/publication.

## Required tests / checks

- `python3 -B scripts/test_workflow_runtime.py -v`
- `python3 -B scripts/test_muse_profile.py -v`
- `python3 -B codex_workflow/runtime/workflow.py validate --package-root codex_workflow --json`
- `python3 -B scripts/package_release.py --output-dir <fresh-output>`
- `python3 -B scripts/package_release.py --verify <fresh-output>/codex_workflow-1.1.18-private.2.zip`
- `sha256sum -c SHA256SUMS` in the fresh output directory
- `git diff --check`
- bounded version-reference readback for the synchronized metadata files

## External write/readback needs

Merge to `main` is the material external write that triggers publication. After merge, verify the GitHub Release/tag is non-draft prerelease `v1.1.18-private.2`, targets the expected integration result, and contains exactly `codex_workflow-1.1.18-private.2.zip` plus `SHA256SUMS`.

Production Workstation update is explicitly outside this Card and requires separate user authorization later.

## Independent review

`RECOMMENDED` — the metadata change is mechanically small, but merge triggers an externally visible, effectively immutable release/tag; independent verification should precede publication.
