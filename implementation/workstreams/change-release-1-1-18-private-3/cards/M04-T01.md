# M04-T01 — Prepare owner release 1.1.18-private.3

- Milestone: `M04`

> Stable Task Card contract. Mutable execution/review/result state lives only in the selected workstream Task Board.

## Authority slice

- Master Plan / milestone contract:
  - `planning/MASTER_PLAN.md#4-milestones` — M04 release-readiness outcome;
  - `planning/MASTER_PLAN.md#7-deployment--migration--rollback-strategy`;
  - explicit release authorization boundary in the approved plan.
- Requirements: `requirements/REQUIREMENTS.md` — owner-release channel constraint and REQ-021 selective-adoption boundary.
- Accepted decision: `decisions/DEC-005-fork-release-version-generation.md`.
- Release procedure: `RELEASING.md`.
- Relevant OpenSpec: none.
- Accepted dependency result: current reviewed/merged `main` at `6ce308a6dd7c92136ed01037800b61ba974d22b3`.

### Must preserve

- The SemVer generation remains 1.1.18 and the next private release is exactly `1.1.18-private.3`.
- Normal owner releases come from reviewed `main` through the repository Release workflow and verified release assets.
- This Card packages the already-reviewed current source; it must not introduce runtime behavior changes.
- Historical release/source provenance remains truthful and is not blindly rewritten.
- Release metadata checked by `.github/workflows/release.yml` remains synchronized.
- User authorization covers release publication only; production Workstation update remains separate.

### Must not / rationale that must travel

- Do not install or update the production Workstation in this workstream.
- Do not manually create a tag/release to bypass the normal VERSION-triggered Release workflow.
- Do not publish from an unreviewed candidate branch.
- Do not change runtime behavior, capability semantics, compute profiles, update-channel behavior, or unrelated documentation.

## Dependencies

- Current reviewed/merged `codex_workflow/main` source at branch base.
- Explicit user authorization for release publication: satisfied by the current request.

## Outcome

Prepare a reviewable release-only branch whose merge to `main` will publish `1.1.18-private.3` through the normal verified GitHub Actions release path.

## Scope

### Included

- Set `codex_workflow/operate/VERSION` to `1.1.18-private.3`.
- Synchronize the version marker in `codex_workflow/operate/user_AGENTS.md`.
- Synchronize current-version text in `README.md`.
- Reconcile `RELEASING.md` current-version text, package examples, expected assets, and release-workflow description to `1.1.18-private.3`.
- Advance version-specific regression expectations from current `.2` / next `.3` to current `.3` / next `.4`.
- Run release-equivalent non-publishing verification through repository CI and exact-subject checks.
- Prepare PR and durable review evidence before merge.

### Excluded

- Runtime/source behavior changes.
- Production Workstation deployment/update.
- New credentials, secrets, capability configuration, or Muse state mutation.
- Manual GitHub tag/release publication outside the repository Release workflow.

## Acceptance

- All synchronized release metadata surfaces identify current release `1.1.18-private.3`.
- Historical references remain semantically correct.
- No runtime implementation behavior changes are introduced by this Card.
- Runtime and Muse profile regression tests are GREEN.
- Package validation/build/archive verification succeeds for `codex_workflow-1.1.18-private.3.zip` with `SHA256SUMS`.
- Version-specific regression expectations identify current `.3` and next `.4`.
- Independent review is GREEN before merge/publication.

## Required tests / checks

- repository PR Tests workflow;
- `python3 -B scripts/test_workflow_runtime.py -v`;
- `python3 -B scripts/test_muse_profile.py -v`;
- `python3 -B codex_workflow/runtime/workflow.py validate --package-root codex_workflow --json`;
- package build and archive verify for `codex_workflow-1.1.18-private.3.zip`;
- `sha256sum -c SHA256SUMS`;
- `git diff --check`;
- bounded version-reference readback.

## External write/readback needs

Merge to `main` is the material external write that triggers publication. After merge, verify release/tag `v1.1.18-private.3`, expected target commit, non-draft prerelease state, and exactly `codex_workflow-1.1.18-private.3.zip` plus `SHA256SUMS`.

Production Workstation update is explicitly outside this Card.

## Independent review

`RECOMMENDED` — the metadata change is mechanically small, but merge triggers an externally visible, effectively immutable release/tag; independent verification should precede publication.
