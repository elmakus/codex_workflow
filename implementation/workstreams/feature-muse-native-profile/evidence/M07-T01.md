# M07-T01 implementation evidence

Card: `M07-T01 — Native profile/provider rendering foundation`
Implementation subject: `709bcafe3de315bdbc2877d21650ddca2d819fb7`
Draft PR: `#20`
CI: GitHub Actions `Tests` run `35564033356` / run number `295`
CI subject: `709bcafe3de315bdbc2877d21650ddca2d819fb7`
CI conclusion: `success`

## Implemented result

- Added the third `muse-native` compute allocation for all six workflow roles.
- Native allocation resolves to `muse-spark-1.3-contributor`, reasoning effort `max`, native Codex harness, and provider selector `cliproxyapi`.
- Added fail-closed validation of the user-owned `[model_providers.cliproxyapi]` configuration before profile mutation; the check requires a non-empty endpoint and Responses wire API while returning only non-secret compatibility evidence.
- Added workflow-owned worker `model_provider` rendering with an ownership marker; an unrelated/unowned worker provider selector is not overwritten.
- Internal Codex agents are enabled for `plus` and `muse-native`; external `muse-max` keeps its existing dormant-native-worker behavior.
- `muse-native` receives the accepted quiet communication-policy rendering hook without importing the external Muse adapter/session/wait/capability-hint transport.
- Added focused switching, provider-validation, worker-rendering, rollback/preservation, and quiet-policy regression coverage.

## Verification

GitHub Actions run `35564033356` is GREEN for the exact implementation subject. The job completed all repository checks:

- Runtime regression tests — GREEN
- Muse adapter regression tests — GREEN
- Python compile check — GREEN
- Muse Max/profile regression tests, including the new `muse-native` cases — GREEN
- Package validation — GREEN
- Package build — GREEN
- Package archive verification — GREEN

## Scope / safety inspection

Diff inspected from M07-T01 execution-start state `187892e48bb2dbe0b4ef6db8843da90b7798f7a8` through implementation subject `709bcafe3de315bdbc2877d21650ddca2d819fb7`.

Functional implementation/test changes are bounded to:

- `codex_workflow/runtime/compute_profiles.py`
- `scripts/test_muse_profile.py`
- `scripts/test_workflow_runtime.py`

The other changes in that range bind draft PR `#20` into workflow state.

No authentication credential/token value is introduced into implementation or evidence. Tests use a loopback dummy provider endpoint only. Profile logic validates provider shape but does not create, migrate, persist, echo, or remove OAuth credentials.

## Deferred JIT boundary

This Card does not claim the final live OAuth-backed CLIProxyAPI/provider acceptance. Per R4/M07, the exact live provider smoke plus update/switch preservation successor is materialized JIT after this implementation result is independently accepted.
