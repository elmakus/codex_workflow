# M07-T02 implementation evidence

Card: `M07-T02 — Native update preservation and live provider route smoke`

## Repository result

Implementation subject before durable evidence/spec reconciliation: `b2291cc64210a15fb63cc1f05fccf08395e3193d`.

Implemented:
- update/runtime materialization now validates every provider required by the selected compute profile before returning an update plan;
- `muse-native` update requires the user-owned `cliproxyapi` provider with a non-empty endpoint and Responses wire API and fails closed before applying mutations when that route is missing/incompatible;
- update keeps native Codex agents enabled for both `plus` and `muse-native`, while external `muse-max` remains disabled;
- selected `muse-native` persists across update with all six worker TOMLs retaining `muse-spark-1.3-contributor`, reasoning `max`, and `model_provider = "cliproxyapi"`;
- unrelated provider/config state remains preserved.

Focused regression coverage now proves:
- selected `muse-native` survives update with exact worker/provider/agent-enable state;
- missing `cliproxyapi` and incompatible `wire_api` reject the update plan without changing config, selected-profile state, or worker TOMLs;
- existing `muse-max` update preservation remains GREEN.

## Verification

Exact branch clone at `b2291cc64210a15fb63cc1f05fccf08395e3193d` ran the repository workflow commands successfully:

- runtime regression suite: 101/101 GREEN;
- Muse adapter suite: 39/39 GREEN;
- focused profile suite: 14/14 GREEN;
- Python compile check: GREEN;
- package validate: GREEN;
- package build: GREEN;
- archive verification for `1.1.18-private.3`: GREEN.

GitHub Actions run `35569162187` on the preceding test commit exposed only a test-fixture package-marker mismatch. The fixture was corrected in `b2291cc64210a15fb63cc1f05fccf08395e3193d`; the exact full command matrix above then passed.

## Installed-environment provider smoke

The existing installed environment was inspected without reading or changing credential material.

Observed:
- workstation container `chatgpt-ce-workstation` is running Codex Desktop; its Codex binary exists at `/opt/codex-desktop/resources/codex`;
- its user config `/home/codex/.codex/config.toml` currently has no `[model_providers.cliproxyapi]` route;
- the running `cli-proxy-api` service is reachable at the installed host endpoint, but an unauthenticated `GET /v1/models` returns HTTP 401;
- no CLIProxyAPI client key, Muse/Meta OAuth token, login/device-flow state, or other credential was read, copied, printed, created, rotated, persisted, or repaired.

Smoke classification: **external-environment/configuration blocker, fail-closed**. A real Codex request pinned to `cliproxyapi` / `muse-spark-1.3-contributor` / `max` cannot be executed from the current workstation without supplying/configuring user-owned client/provider credential state. No fallback provider/model/effort was attempted.

This is the Card's permitted blocker outcome for the live sub-obligation and leaves credential setup outside repository ownership.
