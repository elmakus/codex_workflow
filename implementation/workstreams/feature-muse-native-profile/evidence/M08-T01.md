# M08-T01 implementation evidence

Card: `M08-T01 — Native orchestration contract materialization`

## Repository result

Implementation subject before durable evidence/OpenSpec reconciliation: `92975fedd29e198c292f6da072eebab546e2bc89`.

Implemented without adding a new scheduler/transport layer:
- `muse-native` is explicitly bound to the normal internal Codex worker lifecycle for all six roles while retaining the M07 Muse model/reasoning/provider allocation;
- Heavy and Delegation make native no-poll `wait_agent`, same-owner continuation/repair, fresh `fork_turns="none"` contexts, Tester independence and exact-three Investigator semantics apply to `muse-native`;
- native Material Event Push now covers `plus` and `muse-native`, while external `muse-max` remains outside the internal `send_message` channel;
- native MCP/tool/skill guidance uses the Codex worker capability plane and fails visibly when a required capability is unavailable; it does not translate native work into `MuseCapabilityHints` or `runtime/muse_worker.py`;
- installed dispatch/profile guidance exposes `muse-native` and explicitly forbids routing it through the external Muse worker lifecycle;
- external `muse-max` one-cell wait/session/adapter/capability-hint behavior remains scoped to that profile;
- no runtime scheduler/session/provider implementation code changed in this Card.

Functional diff from the M08-T01 start state changes only:
- `codex_workflow/AGENTS.md`
- `codex_workflow/heavy_route.md`
- `codex_workflow/delegation.md`
- `codex_workflow/operate/user_AGENTS.md`
- `codex_workflow/operate/profile.md`
- `scripts/test_muse_profile.py`
- `scripts/test_workflow_runtime.py`

## Exact-subject verification

A fresh branch clone on the connected `Tower` environment was pinned to `92975fedd29e198c292f6da072eebab546e2bc89` and ran:

- `python3 -B scripts/test_muse_profile.py -v`: **15/15 GREEN**;
- `python3 -B scripts/test_workflow_runtime.py -v`: **101/101 GREEN**;
- `python3 -B scripts/test_muse_adapter.py -v`: **39/39 GREEN**;
- Python compile check: **GREEN**;
- package validation: **GREEN**;
- release package build for `1.1.18-private.3`: **GREEN**;
- archive verification: **GREEN**.

The first package-build attempt in the temporary verification clone correctly failed because the preceding compile step had generated `__pycache__` inside that clone. After deleting only those generated cache artifacts, `git status --porcelain` was empty and build/archive verification passed on the same immutable commit. No repository source was altered by that cleanup.

## Scope/readback

- No live OAuth/provider/account configuration was created or changed.
- No live M08 lifecycle/MCP/skill success is claimed by this Card.
- The downstream live M08 obligation remains JIT and must use the exact installed native Codex/provider surface after this Card is independently accepted.
- PR #20 remains draft and currently requires later integration refresh against moved `main`; that cross-branch state is outside this same-branch Card acceptance and does not alter the exact-subject verification above.
