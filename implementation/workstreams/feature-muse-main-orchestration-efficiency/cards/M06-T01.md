# M06-T01 — Implement quiet milestone communication and prove integrated Muse acceptance

- Milestone: `M06`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md` R3 — M06, verification strategy, authorization boundaries and JIT/deferred decomposition.
- Requirements: `requirements/REQUIREMENTS.md` — REQ-025, REQ-026, REQ-027, REQ-029 plus integrated acceptance preservation for REQ-022 through REQ-028.
- Accepted decision: `decisions/DEC-006-muse-event-driven-waiting.md`.
- Accepted predecessor checkpoint: M05 GREEN in `implementation/workstreams/feature-muse-main-orchestration-efficiency/evidence/M05-acceptance.md` and `handoffs/M05_HANDOFF.md`.
- Relevant OpenSpec: `none` — the communication behavior and wait invariants are explicit in accepted requirements/DEC-006 and do not introduce a separate runtime API.

### Must preserve

- M05 native one-cell `muse-max` launch/await contract and zero-periodic-Main-sampling invariant.
- Existing Muse timeout/cancellation cleanup, logical-session binding, fail-closed resume/replacement, Executor/Tester independence and private raw-artifact isolation.
- Existing `plus` communication, internal-worker lifecycle, long `wait_agent` and Material Event Push semantics.
- Main remains orchestration owner; Muse remains the worker runtime under `muse-max`.
- Release/version/tag/GitHub Release and external deployment boundaries remain unchanged.

### Must not / rationale that must travel

- Do not restore historical hard-silent orchestration.
- Do not hide blockers, immediate risk/authorization needs, material scope/architecture changes or the normal final result.
- Do not emit routine worker-start/wait/status/session/recovery/Git bookkeeping or liveness-only commentary under `muse-max`.
- Do not let a timer/heartbeat/liveness-only update wake Main or create a model turn.
- Do not weaken or replace the M05 event-driven wait contract merely to implement communication policy.
- Do not change `plus` semantics.

## Dependencies

- M05 terminal GREEN checkpoint.

## Outcome

Make `muse-max` use the accepted quiet milestone communication policy while retaining useful phase visibility and re-proving the complete wait-cost invariant under the final communication policy.

## Scope

### Included

- Profile-owned `muse-max` communication policy rendered by `codex_workflow/runtime/compute_profiles.py`.
- Active profile/user-facing guidance that describes the two communication policies, including `codex_workflow/AGENTS.md`, `codex_workflow/operate/profile.md` and README material where applicable.
- Focused profile/rendering tests that distinguish quiet milestone `muse-max` behavior from both `plus` restrained communication and historical hard silence.
- Preservation checks for the M05 one-cell event-driven wait instructions.
- Existing affected Muse adapter/profile regressions.
- One real multi-minute Muse worker invocation through the final `muse-max` rendered policy, with matching Codex-LB and rollout evidence.
- Sanitized durable M06 evidence sufficient for independent review and final milestone/workstream acceptance.

### Excluded

- Changes to `plus` orchestration semantics.
- Muse runtime/session redesign.
- New progress timers, heartbeat channels or managed event brokers.
- VERSION/tag/GitHub Release or external deployment changes.
- Raw private Muse trajectories, credentials or unrelated logs.

## Acceptance

- Rendered `muse-max` Heavy communication policy explicitly suppresses routine worker-start/wait/status/session/recovery/Git bookkeeping and liveness-only narration.
- It permits concise user-meaningful phase transitions and requires blockers, immediate risk/authorization needs, material scope/architecture changes and the normal final result to remain visible.
- It is materially quieter than the prior normal-concise `muse-max` policy but is not hard silence.
- `plus` rendered communication text and behavior remain unchanged.
- Active AGENTS/profile/README guidance is consistent with the quiet-milestone distinction.
- No timer/heartbeat/liveness-only policy can wake Main solely to report healthy progress.
- The M05 one-cell event-driven wait contract remains present and regression-visible.
- Existing Muse timeout/cancellation/session/recovery/logical-session/private-artifact invariants remain GREEN in applicable tests.
- A real Muse worker remains healthy for at least several minutes and completes normally under the final rendered policy.
- For that live run, Codex-LB has no periodic Main requests between dispatch and terminal/material completion, excluding explicit user input or genuinely material events.
- Matching Codex rollout has one outer Code Mode `exec` ownership interval and no recurring top-level Main-driven liveness wait loop.
- Durable evidence records the exact implementation subject, tests and sanitized live identifiers/readback.

## Required tests / checks

- Exact diff/readback proving changes are bounded to communication/profile/docs/tests plus durable state/evidence.
- `python3 scripts/test_muse_profile.py`.
- `python3 scripts/test_muse_adapter.py`.
- Focused rendering assertions for both `plus` and `muse-max`.
- Real multi-minute Muse-worker run through the final active `muse-max` path.
- Codex-LB request-count readback for that exact live conversation.
- Matching Codex rollout/tool-call inspection.
- Post-run process/repository readback.

## External write/readback needs

Authorized local workstation verification may create bounded transient Codex/Muse processes, logical test sessions and private runtime artifacts. Persist only sanitized evidence required for review. No external deployment/release write is authorized.

## Independent review

`RECOMMENDED` — this Card changes the user-visible `muse-max` orchestration policy and carries the final integrated live acceptance for the feature.

## Contract overrides

None.
