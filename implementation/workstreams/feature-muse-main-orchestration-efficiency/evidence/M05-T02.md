# M05-T02 — Native Muse event-driven await implementation evidence

Date: 2026-09-20  
Card: `M05-T02`  
Implementation base: `b55f127cd09aed76d582d8c10039683c429afa4c`  
Implementation result / review subject: `commit:1c0c0bf073308b8e87c009652e1192422445862c`

## Result

GREEN implementation/acceptance evidence for the M05-T02 execution role. The exact implementation subject is frozen for fresh independent review; this evidence does not issue the independent verdict.

The accepted native path is now explicit in the active `muse-max` Main-facing contracts:

- `codex_workflow/heavy_route.md` requires each bounded Muse turn to be launched and awaited inside one outer Code Mode `exec` cell.
- `codex_workflow/delegation.md` defines the native await shape: one `tools.exec_command`, retained terminal session identity, and any subsequent `tools.write_stdin` waits inside the same outer cell until terminal/material completion.
- The active contract rejects repeated top-level Main-driven liveness/status waits, timers/heartbeats and unmanaged background-process polling.
- `scripts/test_muse_profile.py` carries a focused regression for those active instructions.

No production Muse adapter/session source and no `plus` orchestration source were changed by the implementation subject.

## Exact implementation diff

Git compare `b55f127cd09aed76d582d8c10039683c429afa4c..1c0c0bf073308b8e87c009652e1192422445862c` is exactly 3 commits and 3 files:

- `codex_workflow/delegation.md`: +24
- `codex_workflow/heavy_route.md`: +2
- `scripts/test_muse_profile.py`: +17

No other source path is in the implementation diff.

## Exact-head regressions

Re-run on exact local checkout `1c0c0bf073308b8e87c009652e1192422445862c` with a clean working tree:

- `python3 scripts/test_muse_profile.py` → 8/8 GREEN.
- `python3 scripts/test_muse_adapter.py` → 39/39 GREEN.

The profile regression includes the unchanged `plus` allocation/harness checks. The adapter suite preserves existing timeout/cancellation cleanup, session lifecycle/binding, fail-closed recovery and artifact/runtime invariants exercised by that suite.

## Real Muse acceptance environment

The accepted live run used an isolated temporary HOME/CODEX_HOME materialized from the exact candidate branch while reusing the workstation's existing Muse authentication. Readback confirmed:

- active compute profile: `muse-max`;
- Muse Code: `1.3.0`;
- candidate Heavy contract contains the one-cell requirement;
- candidate Delegation contract contains `Muse event-driven await`.

An earlier setup run was discarded after readback proved it had loaded the workstation's older global `~/.codex` workflow instead of the candidate materialization. It is not used as acceptance evidence.

Before the accepted run, the isolated runtime correctly failed closed with `session_busy` because an earlier discarded probe left an inactive A1 session mapping on the test workspace. Main verified that no Muse process owned that mapping and retired only that exact predecessor through the workflow's public `retire_worker_session` lifecycle API. No registry file was manually edited. This is supporting recovery/fail-closed evidence, not part of the healthy acceptance interval.

## Accepted real Muse run

Codex Main conversation:

- thread: `01a0c017-4283-7412-b0b3-e73a74cb0f28`
- model/reasoning: `gpt-5.6-sol / medium`
- repository Git subject visible in rollout: `1c0c0bf073308b8e87c009652e1192422445862c`

Muse worker:

- Task ID: `M05-T02-LIVE`
- logical worker: `M05-T02-A2`
- caller scope: `M05-T02:live-valid`
- invocation: `e4c388dc-ad60-4410-a25d-0a83feaf674f`
- session: `78dfcf7b-12fb-4d9a-b5f0-c8684013db52`
- terminal status: `completed`
- process exit code: `0`
- terminal session state: `ready`
- repository changed paths reported by the normalized adapter result: `[]`
- required foreground worker command: `sleep 190`
- exact terminal marker: `M05_REAL_MUSE_WAIT_OK`

The normalized result states that the Muse worker executed the foreground `sleep 190` to completion before returning its final report.

## Codex rollout verification

Matching rollout:

`/tmp/m05-t02-home/.codex/sessions/2026/09/20/rollout-2026-09-20T20-32-24-01a0c017-4283-7412-b0b3-e73a74cb0f28.jsonl`

The successful A2 dispatch is one top-level `custom_tool_call` named `exec`. Its cell contains:

- one `tools.exec_command` launching `runtime/muse_worker.py`;
- local retention of the returned terminal session;
- a `while (r.session_id !== undefined)` loop;
- `tools.write_stdin` with empty input and `yield_time_ms: 300000` inside that same outer cell.

The outer cell reports wall time `226.3 seconds` and returns only the terminal normalized Muse result. There is no recurring top-level Main-driven liveness wait loop for the successful A2 interval.

## Codex-LB request-count verification

For the same Main conversation, the request immediately before the healthy A2 wait completed at:

- request `129354`: `2026-09-20 18:34:29.660004 UTC`, `gpt-5.6-sol / medium`, success.

The next Main request did not occur until:

- request `129355`: `2026-09-20 18:38:26.010823 UTC`, `gpt-5.6-sol / medium`, success.

There are no intervening Main requests. The request gap is about 236.35 seconds, covering the complete successful outer-cell Muse wait. This independently confirms zero periodic Main/model sampling during healthy liveness.

## Post-run readback

- A2 durable registry record remains bound to the expected logical worker/caller scope/session/task/workspace with state `ready`.
- Process readback after completion shows no surviving process matching `M05-T02-A2`.
- The canonical remote branch implementation head is exactly `1c0c0bf073308b8e87c009652e1192422445862c`.
- The exact implementation checkout used for final regression rerun was clean before the tests.

## Acceptance mapping

- One-cell active contract: GREEN.
- Internal terminal waiting owned by the same cell: GREEN.
- No Main-driven healthy-liveness polling/timers/background substitute: GREEN.
- Existing adapter/session/cancellation regressions: GREEN, 39/39.
- Focused profile/documentation contract regression: GREEN, total profile suite 8/8.
- Real Muse worker healthy for several minutes and terminally completes: GREEN.
- Codex-LB zero periodic Main requests across healthy interval: GREEN.
- Matching rollout shows one successful outer `exec` with internal `exec_command/write_stdin` ownership: GREEN.
- `plus` source/profile behavior unchanged by implementation and profile regression remains GREEN.
- No release/version/tag/GitHub Release or external deployment action occurred.

## Review boundary

M05-T02 remains non-terminal until the REQUIRED/RECOMMENDED review gate for exact subject `commit:1c0c0bf073308b8e87c009652e1192422445862c` is independently GREEN.
